# TODO: Filter Appointments by Day with Arrows

1. [x] Update `AppointmentState` to include `selectedDate` field.
2. [x] Add `NextDayEvent` and `PreviousDayEvent` to `AppointmentEvent`.
3. [x] Update `AppointmentBloc` handlers for new events and set initial `selectedDate` on fetch.
4. [x] Update `AppointmentsListScreen` UI: arrow buttons, dynamic date label, list filtering.


