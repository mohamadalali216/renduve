import 'package:flutter/material.dart';

class AppTextStyles {
  static const String fontFamily = "roboto"; // اسم الخط

  // حجم كبير (مثلاً للعناوين الرئيسية)
  static const TextStyle bold_24 = TextStyle(
    fontFamily: fontFamily,
    fontSize: 24,
    fontWeight: FontWeight.bold,
  );

  // حجم متوسط (مثلاً للعناوين الثانوية أو الأزرار)
  static const TextStyle bold_16 = TextStyle(
    fontFamily: fontFamily,
    fontSize: 16,
    fontWeight: FontWeight.bold,
  );

  // حجم صغير (مثلاً للملاحظات أو النصوص الثانوية)
  static const TextStyle bold_12 = TextStyle(
    fontFamily: fontFamily,
    fontSize: 12,
    fontWeight: FontWeight.bold,
  );

  // حجم أصغر (مثلاً للـ captions أو الوقت)
  static const TextStyle bold_10 = TextStyle(
    fontFamily: fontFamily,
    fontSize: 10,
    fontWeight: FontWeight.bold,
  );

  // حجم صغير (مثلاً للنصوص العادية)
 
}
